# 复制为仓库根目录 .env 后再替换两个密码；真实 .env 不提交到 Git。
MYSQL_PASSWORD=replace-with-a-long-random-password
MYSQL_ROOT_PASSWORD=replace-with-a-different-long-random-password
# 浏览器访问前端时使用的完整 origin，同时用于后端 CORS 和 SvelteKit 校验。
FRONTEND_ORIGIN=http://8.136.60.154:18081
